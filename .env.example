# AI Chat App - Environment Variables Example
# Copy this to .env and fill in your values (never commit .env to git)
#
# NOTE: The Anthropic API key is stored via flutter_secure_storage (device keychain/keystore)
# and entered through the onboarding screen. No API key goes in this file.
#
# This file is for future server-side or additional configuration only.

APP_ENV=development
